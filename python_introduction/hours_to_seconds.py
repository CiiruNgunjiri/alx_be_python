# python script that converts a specific number of hours into seconds
# define the variables and give value

hours = 2
minutes_per_hour = 60
seconds_per_minute = 60

seconds_per_hour = minutes_per_hour * seconds_per_minute

# calculate the seconds found in the variable 'hours'

seconds = hours * 3600

# print the calculated seconds in the format: [hours]hour(s) is [seconds] seconds.

print (f"{hours} hour(s) is {seconds} seconds.")
